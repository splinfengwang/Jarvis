---
name: jarvis-knowledge-extract
description: 从 Topic 或会话中生成 Evidence Pack 和确认清单。用于"萃取一下这个 Topic""整理可入库知识"。不自动入库。
next_skills:
  - jarvis-knowledge-ingest
---

# jarvis-knowledge-extract

trigger:
- "萃取一下这个 Topic"
- "整理一下可入库知识"
- "把这个话题提炼一下"

non_trigger:
- 用户直接确认某组入库时 → `jarvis-knowledge-ingest`。
- 没有 Topic 或来源时，不继续抽象。

inputs:
- Topic 索引、快照、讨论记录。
- 可选 JSONL 原始会话。
- 相关源文件。

outputs:
- Evidence Pack 列表。
- 确认清单。
- 缺失来源清单。
- 不入库声明。

required_references:
- `jarvis/references/evidence-pack-spec.md`

on_demand_references:
- `jarvis-knowledge-model`
- `jarvis/references/knowledge-extraction.md`
- `jarvis/research-cards/PlugMem.md`
- `jarvis/research-cards/ByteRover.md`
- 仅在解释 Evidence Pack 来源、发生证据追溯失败或修改萃取协议时读取。

allowed_scripts:
- `jarvis/scripts/extract_evidence_pack.py`
- `jarvis/scripts/locate_session_jsonl.py`
- `jarvis/scripts/validate_links.py`
- `jarvis/scripts/knowledge_link_stats.py`

write_level:
- none

confirmation_rules:
- 只生成候选，不写知识库。
- 医学参数、判断类、产品承诺必须标为需确认。
- 若调用脚本，默认只生成预览；不自动进入入库。

fallback_rules:
- JSONL 找不到时标缺失，不伪造原始会话路径。
- 来源不可追溯的 claim 不进入确认清单。

---

## 萃取流程（5 步）

### 第 1 步：定位源记录

读取 Topic 的 `_上下文快照.md` → 找到"关联会话"表 → 按工具和日期定位 JSONL 文件。

- JSONL 路径明确 → 标记 `✅`
- 只有会话标识无路径 → 按工具映射推断候选，标 `⚠️ 会话文件需确认`
- 文件缺失 → 标 `⚠️ 会话文件缺失`，不阻塞其他来源
- 快照中无关联会话 → 声明"缺失会话记录"，仅从 Topic 文件萃取

**来源清单格式：**

```
来源 1: [工具] [会话ID] → JSONL路径 — ✅/⚠️
来源 2: Topic 文件（索引/快照/讨论记录） — ✅
```

---

### 第 2 步：抽取证据块

从 JSONL 和 Topic 文件中标识以下类型的证据。每条证据保留来源定位（文件路径 + 行号/消息片段）。

| 证据类型 | 来源 | 示例 |
|---------|------|------|
| 用户定义 | 用户对术语/角色/规则的明确描述 | "K-S100Pro 是 S 系列的呼吸管理仪" |
| 用户决策 | 用户对范围/方案/优先级的拍板 | "PPT 只讲氧疗，不讲无创通气" |
| 用户纠正 | 用户指出 Agent 理解错误或补充正确说法 | "不是 K-S100，是 K-S100Pro" |
| 原始文件 | PDF/PPT/文档等路径或消息附件 | "详见 宣传彩页-v0.7.pptx" |
| OCR/文档解析 | OCR、视觉识别、文档解析工具的原始 tool_result | OCR 输出的设备参数表 |
| Agent 结构化整理 | Agent 对文件/流程/方案的结构化摘要 | "压力滴定流程分为 5 个阶段" |
| Agent 推断/归纳 | Agent 基于多个材料做出的判断或趋势 | "基于 FeCO₂ 和 EtCO₂ 的使用差异，推断..." |
| Topic 记录 | 索引、快照、讨论记录、阶段产出 | 讨论记录中的方案对比表 |

**执行：** 对每条证据，标注：
```
[证据] 类型: [用户定义/用户决策/…]
       内容: [一句话]
       来源: [文件路径 + 消息位置]
```

---

### 第 3 步：生成候选知识

将证据块转化为候选知识。每条候选**必须写成可判断真假的句子**。禁止"讨论了 XX""涉及 XX"。

| 类型 | 含义 | 示例 |
|------|------|------|
| L1 术语 | 一个业务概念是什么 | "K-S100Pro 是 S 系列呼吸管理仪，旁流 EtCO₂ 自动监测" |
| L2 关系/规则 | 概念之间怎么协作 | "FeCO₂ 用于结果评定，EtCO₂ 仅用于过程控制" |
| L3 判断/决策 | 为什么这样选，放弃什么 | "PPT 只讲氧疗，不讲无创通气——因为受众是基层医生" |
| L4 待验证问题 | 重要但尚无答案 | "FeCO₂ 与 PaCO₂ 的锚定差值在长期治疗中是否稳定？" |
| F 文件处理产物 | 对源文件的结构化整理 | 产品能力清单、模块表、流程表 |

**执行：** 对每条候选，填写：
```
[候选] 类型: L1/L2/L3/L4/F
       我们的理解: [一句可判真假的判断句]
       准备用法: [以后在哪类任务中用]
```

---

### 第 4 步：信任分级 + 确认清单

根据证据来源分配信任级别，决定建议动作：

| 来源 | 信任级别 | 入库规则 |
|------|---------|---------|
| 用户明确确认/纠正 | 最高 | 可作为确认依据 |
| 原始文件可核对 | 一手证据 | 可入候选，关键参数待确认 |
| OCR/文档解析 tool_result | 高优先 | 优先于 Agent 二次总结，关键数字需回看原文 |
| Agent 结构化整理 | 候选整理 | 可入清单，不直接当事实 |
| Agent 推断/归纳 | 候选判断 | 必须标注推断，需确认 |
| 无法回溯来源 | 不可信 | **不入库** |

**缺字段不放过：** 没有证据来源 → 不进入清单；准备用法说不清 → 不入库；涉及医学参数 → 默认待验证；只有 Agent 推断 → 不能建议直接确认。

---

### 第 5 步：输出确认清单

清单分 **A/B/C/D/E 五组**，组名就是用户的确认指令。

```
# 萃取确认清单: <Topic 名称>

> 来源 Topic: [[路径]]
> 来源清单: [第 1 步的结果]

## A. 可快速确认
适合批量确认的描述型知识。来自用户明确说法、源文件或 OCR，不含重大判断。

| # | 我们的理解 | 类型 | 准备用法 | 证据来源 | 建议动作 |
|---|---|---|---|---|---|
| D1 | … | 描述 | … | 源文件 + OCR | 建议确认 |

## B. 需要拍板
涉及范围边界、优先级、产品判断。

| # | 我们的理解 | 影响范围 | 证据来源 | 需要判断 |
|---|---|---|---|---|---|
| J1 | … | … | 用户消息 + Agent 归纳 | 是否作为默认判断 |

## C. 需要验证
涉及医学/安全参数、来源不足或存在矛盾。

| # | 我们的理解 | 风险 | 证据来源 | 当前处理 |
|---|---|---|---|---|---|
| M1 | … | 医学参数 | OCR/文献 | 待医学验证 |

## D. 建议不入库
过程想法、废弃方案、价值不明确。

| # | 内容 | 原因 |
|---|---|---|
| X1 | … | 已被后续讨论废弃 |

## E. 共享资料索引

| # | 原始资料 | 已找到的处理产物 | 证据等级 | 建议动作 |
|---|---|---|---|---|---|
| F1 | … | OCR + Agent 整理 | 源文档抽取证据 | 建议确认后入库 |

> **可用确认指令**: A 组全确认 / B1 改成… / C 组全部待验证 / D 组不入库 / 全部描述级确认，判断类先待确认
```

**输出后等待用户确认。不自动入库。** 用户确认后 → `jarvis-knowledge-ingest`。

---

## 硬性规则

| 规则 | 违反后果 |
|------|---------|
| 候选必须写成可判真假的句子 | "讨论了 XX" → 不合格 |
| 没有证据来源 → 不进入清单 | 来源缺失的 claim 不入库 |
| Agent 推断/归纳 → 必须标推断，不能建议直接确认 | 推断当事实 → 用户误确认 |
| 医学参数/安全边界 → 默认待验证 | 跳过验证 → 风险 |
| 文件处理产物 → 拆分，不整体入库 | OCR 原文 / Agent 整理 / Agent 推断 必须分开 |
| 输出确认清单后 → 等用户确认，不自动进入入库 | 跳过确认 → 违反 write_level |

acceptance_cases:
- "萃取一下这个 Topic" → 输出 5 步完整的 Evidence Pack 和确认清单。
- 缺 JSONL → 标 `⚠️ 会话文件缺失`，仍从 Topic 文件萃取。
- 候选知识以可判真假的判断句呈现，不出现"讨论了 XX"。
- 每条候选标注证据来源 + 信任级别。
- A 组可批量确认，B/C 组单独确认，D 组不入库。